syntax = "proto3";
option java_multiple_files = true;

package examples.proto; // Requried to generate valid code.

message ZipCode {
  string code = 1;
}

message Address {
  string city = 1;
  ZipCode zip_code = 2;
}
